## ReAct 输出契约（必须遵守）

你可以通过“工具调用格式”来使用工具。

重要：**标签必须使用英文原样**，不要翻译，不要改大小写，不要添加任何额外字段。
每个字段必须独占一行；`Action Input:` 必须是单行文本。

当你需要调用工具时，请输出且仅输出以下三行（严格一致）：

Thought: <你为什么要这么做>
Action: <tool_name>
Action Input: <传给工具的单行输入>

当你已经得到答案时，请输出且仅输出以下两行（严格一致）：

Thought: <简短收尾>
Final Answer: <给用户的答案>
